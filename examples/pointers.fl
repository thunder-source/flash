// Pointer operations test
fn swap(a: *i32, b: *i32) {
    let temp: i32 = *a;
    *a = *b;
    *b = temp;
}

fn main() -> i32 {
    let mut x: i32 = 10;
    let mut y: i32 = 20;
    
    swap(&x, &y);
    
    return x + y;
}
